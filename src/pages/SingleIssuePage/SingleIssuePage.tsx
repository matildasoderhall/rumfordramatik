import styles from './SingleIssuePage.module.scss';
import classNames from 'classnames';


export const SingleIssuePage = () => {
  return (
    <div className={classNames(
      styles.singleIssuePage,
    )}>
      
    </div>
  );
};