export enum ArrowType {
    singleCurved = 'single-curved',
    doubleCurved  = 'double-curved',
    single = 'single',
}

export enum ArrowDirection {
    left = 'left', 
    right = 'right', 
    flip = 'flip',
    normal = 'normal',
}